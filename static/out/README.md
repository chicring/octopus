# Static Output Placeholder

This file keeps `static/out` present for Go embed during local tests before the frontend is built.

The release build replaces this directory with the generated Next.js static export.
